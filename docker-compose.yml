services:
  concrecol-web:
    build:
      context: .
      dockerfile: Dockerfile
      secrets:
        - app_env
    container_name: concrecol-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"

secrets:
  app_env:
    file: .env
